[package]
name = "rx-rest"
version = "0.1.0"
authors = ["dayn9t <dayn9t@gmail.com>"]
edition = "2024"

[dependencies]
rx-core = { version = "*", path = "../rx-core" }
rx-db = { version = "*", path = "../rx-db" }

serde = "*"
serde_json = "*"
serde_derive = "*"
toml = "*"

time = { version = "*", features = ["macros", "formatting"] }
poem = { version = "3", features = ["anyhow"] }
poem-openapi = { version = "5.0.3", features = ["swagger-ui", "time", "uuid", "geo", "url"] }
#poem-openapi = { version = "5.1.10", features = ["swagger-ui", "uuid", "geo", "url"] }
tokio = { version = "1", features = ["full"] }

anyhow = "*"
thiserror = "*"
